name: "Criar Branch de Release"

on:
  workflow_dispatch:
    inputs:
      increment_type:
        description: "Tipo de incremento de versão (major, minor, patch, alpha)"
        required: true
        default: patch
        type: choice
        options:
          - major
          - minor
          - patch
          - alpha

jobs:
  create-release-branch:
    name: Criar Release Branch
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # para obter todas as tags e histórico

      - name: Configurar usuário Git
        run: |
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor }}@users.noreply.github.com"

      - name: Encontrar a última versão
        id: find_last_version
        run: |
          all_versions=$(git ls-remote --refs --sort='v:refname' origin 'refs/tags/release/*' 'refs/heads/release/*' | awk -F'/' '{print $NF}' | tr '\n' ' ')

          highest_version=$(echo "$all_versions" | tr ' ' '\n' | sort -V | tail -n1)

          if [ -z "$highest_version" ]; then
            highest_version="0.0.0"
          fi

          echo "latest_version=$highest_version" >> $GITHUB_OUTPUT
          echo "🔖 Última versão semântica encontrada: $highest_version"

      - name: Calcular nova versão e branch
        id: bump_version
        run: |
          version="${{ steps.find_last_version.outputs.latest_version }}"
          increment_type="${{ github.event.inputs.increment_type }}"
          
          echo "🔨 Gerando nova versão a partir de tag: $version"
          echo "➡️  Tipo de incremento: $increment_type"

          base_version=$(echo "$version" | cut -d'-' -f1)
          alpha_number=$(echo "$version" | cut -s -d'-' -f2 | sed 's/a//')
          
          declare -i major minor patch
          
          major=$(echo "$base_version" | cut -d'.' -f1)
          if ! [[ "$major" =~ ^[0-9]+$ ]]; then major=0; fi
          
          minor=$(echo "$base_version" | cut -d'.' -f2)
          if ! [[ "$minor" =~ ^[0-9]+$ ]]; then minor=0; fi
          
          patch=$(echo "$base_version" | cut -d'.' -f3)
          if ! [[ "$patch" =~ ^[0-9]+$ ]]; then patch=0; fi
          
          echo "🔍 Versão base (major.minor.patch): $major.$minor.$patch"
          
          new_ver=""
          
          case "$increment_type" in
            major)
              echo "🚀 Incrementando a versão MAJOR."
              major=$((major + 1)); minor=0; patch=0
              new_ver="$major.$minor.$patch" ;;
            minor)
              echo "✨ Incrementando a versão MINOR."
              minor=$((minor + 1)); patch=0
              new_ver="$major.$minor.$patch" ;;
            patch)
              echo "🩹 Incrementando a versão PATCH."
              patch=$((patch + 1))
              new_ver="$major.$minor.$patch" ;;
            alpha)
              echo "🧪 Criando/Incrementando a versão ALPHA."
              if [ -z "$alpha_number" ]; then
                alpha_number=1
              else
                alpha_number=$((alpha_number + 1))
              fi
              new_ver="$base_version-a$alpha_number" ;;
            *)
              echo "❗ Incremento inválido: $increment_type"; exit 1 ;;
          esac
          
          new_branch="release/$new_ver"
          
          echo "✅ Nova versão calculada: $new_ver"
          echo "new_version=$new_ver" >> $GITHUB_OUTPUT
          echo "new_branch=$new_branch" >> $GITHUB_OUTPUT

      - name: Criar e push da branch
        run: |
          NEW_BRANCH="${{ steps.bump_version.outputs.new_branch }}"
          BASE_BRANCH="${{ github.ref_name }}"
          
          if git ls-remote --exit-code --heads origin "$NEW_BRANCH"; then
            echo "❌ Erro: A branch de release '$NEW_BRANCH' já existe. Por favor, incremente a versão ou remova a branch existente."
            exit 1
          fi

          git checkout -b "$NEW_BRANCH" "origin/$BASE_BRANCH"
          echo "✅ Nova branch de release '$NEW_BRANCH' criada localmente a partir de '$BASE_BRANCH'."

          git push -u origin "$NEW_BRANCH"
          echo "🎉 Branch '$NEW_BRANCH' enviada para o repositório remoto com sucesso!"
