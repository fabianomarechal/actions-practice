name: "Criar Branch de Release"

on:
  workflow_dispatch:
    inputs:
      increment_type:
        description: "Tipo de incremento de versão (major, minor, patch)"
        required: true
        default: patch
        type: choice
        options:
          - major
          - minor
          - patch

jobs:
  create-release-branch:
    name: Criar Release Branch
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # para obter todas as tags e histórico

      - name: Configurar usuário Git
        run: |
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor }}@users.noreply.github.com"

      - name: Encontrar última tag ou branch de release
        id: find_last_version
        run: |
          latest_tag=$(git tag --list 'release/*' --sort=-v:refname | head -n1)
          latest_branch=$(git for-each-ref --sort=-committerdate refs/heads/release/* --format='%(refname:short)' | head -n1)

          latest_tag_version=$(echo "$latest_tag" | sed 's/release\///' | tr -d '\n')
          latest_branch_version=$(echo "$latest_branch" | sed 's/release\///' | tr -d '\n')
          
          # Remove o prefixo "v" se existir
          latest_tag_version=$(echo "$latest_tag_version" | sed 's/v//')
          latest_branch_version=$(echo "$latest_branch_version" | sed 's/v//')

          # Compara as versões semânticas para encontrar a mais alta
          if [ -z "$latest_tag_version" ]; then latest_tag_version="0.0.0"; fi
          if [ -z "$latest_branch_version" ]; then latest_branch_version="0.0.0"; fi

          if [[ "$(printf '%s\n' "$latest_tag_version" "$latest_branch_version" | sort -V | tail -n1)" == "$latest_tag_version" ]]; then
            echo "latest_version=$latest_tag_version" >> $GITHUB_OUTPUT
            echo "🔖 Última versão encontrada (tag): $latest_tag_version"
          else
            echo "latest_version=$latest_branch_version" >> $GITHUB_OUTPUT
            echo "🔖 Última versão encontrada (branch): $latest_branch_version"
          fi

      - name: Calcular nova versão e branch
        id: bump_version
        run: |
          tag="${{ steps.find_tag.outputs.latest_tag }}"
          increment_type="${{ github.event.inputs.increment_type }}"
          
          echo "🔨 Gerando nova versão a partir de tag: $tag"
          echo "➡️  Tipo de incremento: $increment_type"
          
          version=$(echo "$tag" | sed 's/release\///')
          
          declare -i major minor patch
          
          major=$(echo "$version" | cut -d'.' -f1)
          if ! [[ "$major" =~ ^[0-9]+$ ]]; then major=0; fi
          
          minor=$(echo "$version" | cut -d'.' -f2)
          if ! [[ "$minor" =~ ^[0-9]+$ ]]; then minor=0; fi
          
          patch=$(echo "$version" | cut -d'.' -f3)
          if ! [[ "$patch" =~ ^[0-9]+$ ]]; then patch=0; fi
          
          echo "🔍 Versão atual (major.minor.patch): $major.$minor.$patch"
          
          case "$increment_type" in
            major)
              echo "🚀 Incrementando a versão MAJOR."
              major=$((major + 1)); minor=0; patch=0 ;;
            minor)
              echo "✨ Incrementando a versão MINOR."
              minor=$((minor + 1)); patch=0 ;;
            patch)
              echo "🩹 Incrementando a versão PATCH."
              patch=$((patch + 1)) ;;
            *)
              echo "❗ Incremento inválido: $increment_type"; exit 1 ;;
          esac
          
          new_ver="$major.$minor.$patch"
          new_branch="release/$new_ver"
          
          echo "✅ Nova versão calculada: $new_ver"
          echo "new_version=$new_ver" >> $GITHUB_OUTPUT
          echo "new_branch=$new_branch" >> $GITHUB_OUTPUT

      - name: Criar e push da branch
        run: |
          NEW_BRANCH="${{ steps.bump_version.outputs.new_branch }}"
          BASE_BRANCH="${{ github.ref_name }}"
          
          if git ls-remote --exit-code --heads origin "$NEW_BRANCH"; then
            echo "❌ Erro: A branch de release '$NEW_BRANCH' já existe. Por favor, incremente a versão ou remova a branch existente."
            exit 1
          fi

          git checkout -b "$NEW_BRANCH" "origin/$BASE_BRANCH"
          echo "✅ Nova branch de release '$NEW_BRANCH' criada localmente a partir de '$BASE_BRANCH'."

          git push -u origin "$NEW_BRANCH"
          echo "🎉 Branch '$NEW_BRANCH' enviada para o repositório remoto com sucesso!"
